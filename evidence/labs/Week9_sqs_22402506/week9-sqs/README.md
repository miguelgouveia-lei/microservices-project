The order-service consumer was intentionally stopped to simulate message processing failure. Messages remained in the main queue and would eventually be retried according to the configured RedrivePolicy. After exceeding the maximum receive count, messages would be moved automatically to the Dead-Letter Queue (DLQ). After fixing the consumer, messages could be replayed by reprocessing or moving them back to the main queue.
